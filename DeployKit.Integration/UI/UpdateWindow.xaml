<Window x:Class="DeployKit.Integration.UI.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تحديث متوفر" Height="420" Width="500"
        WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize"
        FontFamily="Segoe UI" FontSize="14"
        Background="#F5F6FA">
    <Window.Resources>
        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="Background" Value="#4F46E5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#4338CA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="SecondaryButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#F3F4F6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,8,0,20">
            <Border Width="56" Height="56" CornerRadius="14" Background="#EEF2FF"
                    HorizontalAlignment="Center">
                <TextBlock Text="📦" FontSize="28"
                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <TextBlock Text="تحديث متوفر!" FontSize="22" FontWeight="700"
                       HorizontalAlignment="Center" Foreground="#1A1B2E"
                       Margin="0,12,0,4"/>
            <TextBlock x:Name="VersionText" FontSize="15"
                       HorizontalAlignment="Center" Foreground="#6B7280"/>
        </StackPanel>

        <!-- Release Notes -->
        <Border Grid.Row="1" Background="#FFFFFF" CornerRadius="8"
                BorderBrush="#E2E5F0" BorderThickness="1"
                Padding="14" Margin="0,0,0,16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="NotesText" TextWrapping="Wrap"
                           FontSize="13" Foreground="#374151" LineHeight="20"/>
            </ScrollViewer>
        </Border>

        <!-- Progress -->
        <StackPanel Grid.Row="2" Margin="0,0,0,12"
                    x:Name="ProgressPanel" Visibility="Collapsed">
            <ProgressBar x:Name="DownloadProgress" Height="6"
                         Background="#E5E7EB" BorderThickness="0"
                         Foreground="#4F46E5" Maximum="100" Minimum="0"/>
            <TextBlock x:Name="ProgressLabel" FontSize="11"
                       Foreground="#9CA3AF" HorizontalAlignment="Center"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Center">
            <Button x:Name="UpdateBtn" Content="⬇️  تحديث الآن"
                    Style="{StaticResource PrimaryButton}"
                    Click="OnUpdateClick"/>
            <Button x:Name="LaterBtn" Content="لاحقاً"
                    Style="{StaticResource SecondaryButton}"
                    Margin="12,0,0,0"
                    Click="OnLaterClick"/>
        </StackPanel>
    </Grid>
</Window>
